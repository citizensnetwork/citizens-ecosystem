# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.next/
.turbo/
out/
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/

# Test / coverage
coverage/
.nyc_output/

# Next.js
# Note: next-env.d.ts is committed so `typecheck` works without a prior `next build`.

# Legacy reference repo, not part of this build
Old version/

# Claude Code local session/offload notes (never committed)
.claude/


# Static HTML frontend — generated build outputs + local runtime config.
# public/ and mobile-dist/ are produced from apps/wear/src/frontend by
# apps/wear/scripts/build-frontend.js on every build; config.js carries
# per-machine values (copy from config.example.js).
apps/wear/public/
apps/wear/mobile-dist/
apps/wear/src/frontend/config.js

.vercel
.env*.local
.pnpm/
