// ════════════════════════════════════════════════════════════════════
//  Citizens Vision — Capacitor bridge STUB
//  ------------------------------------------------------------------
//  Vision is a desktop back-office app with NO native (Capacitor) shell
//  planned. The @citizens/frontend-build pipeline expects this file to
//  exist (Connect/Wear use it to expose @capacitor/* plugins), so this
//  stub satisfies the contract without pulling any Capacitor packages.
//  If a Vision native shell is ever wanted, replace this with the
//  Connect/Wear bridge (imports @capacitor/core + app + browser).
// ════════════════════════════════════════════════════════════════════
window.CapCore = null;
window.CapApp = null;
window.CapBrowser = null;
window.CapGeolocation = null;
